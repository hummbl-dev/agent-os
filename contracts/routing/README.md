# contracts/routing

Canonical routing decision contract for deterministic provider/model selection.

## Status

v0.1 implemented.

## Structure

```
schemas/            # Draft 2020-12 JSON Schema
runtime_validator/  # Schema + runtime invariant checks + canonical types
examples/           # Valid and invalid example routing decisions
tests/              # Pytest contract suite
```
